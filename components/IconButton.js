import { Pressable, StyleSheet, Text } from "react-native";
import  MaterialIcons from "@expo/vector-icons/MaterialIconsl";

export default function IconButton({icon, label, onPress}){
    return(
        <View>
            
        </View>
    )
}